# ========================================
# Flow-Forge Docker 环境变量配置
# ========================================
# 复制此文件为 .env 并修改相应配置
# cp .env.example .env

# ----------------------------------------
# PostgreSQL 连接配置 (必填)
# ----------------------------------------
# 数据库主机地址
# - Windows 宿主机: host.docker.internal
# - Linux 宿主机: 172.17.0.1
# - Docker 容器: 容器名称 (如 flow-forge-postgres)
# - 远程服务器: 实际 IP 或域名
POSTGRES_HOST=your-postgres-host

# 数据库端口
POSTGRES_PORT=5432

# 数据库名称
POSTGRES_DB=flow_forge

# 数据库用户
POSTGRES_USER=flow_forge

# 数据库密码 (请修改为强密码)
POSTGRES_PASSWORD=your_secure_password

# ----------------------------------------
# Redis 配置
# ----------------------------------------
# Redis 密码 (留空表示无密码)
REDIS_PASSWORD=

# Redis 对外暴露端口
REDIS_EXPOSE_PORT=6379

# ----------------------------------------
# MinIO 配置
# ----------------------------------------
# 访问密钥
MINIO_ACCESS_KEY=minioadmin

# 秘密密钥 (请修改为强密码)
MINIO_SECRET_KEY=minioadmin

# 存储桶名称
MINIO_BUCKET=flow-forge

# API 端口
MINIO_API_PORT=9000

# 控制台端口
MINIO_CONSOLE_PORT=9001

# ----------------------------------------
# 应用配置
# ----------------------------------------
# 应用服务端口
SERVER_PORT=8080

# 日志级别 (DEBUG/INFO/WARN/ERROR)
LOG_LEVEL=INFO

# Spring 激活配置
SPRING_PROFILES_ACTIVE=prod

# ----------------------------------------
# JVM 配置 (可选)
# ----------------------------------------
# 最大堆内存百分比 (容器内存的百分比)
# JAVA_OPTS_MAX_RAM=75.0
